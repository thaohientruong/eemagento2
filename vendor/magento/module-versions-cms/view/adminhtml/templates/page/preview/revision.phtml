<?php
/**
 * Copyright © 2016 Magento. All rights reserved.
 * See COPYING.txt for license details.
 */

// @codingStandardsIgnoreFile

/* @var $block \Magento\VersionsCms\Block\Adminhtml\Cms\Page\Preview\Revision */
?>

<?php if ($data = $block->getRevisions()): ?>
<label for="revision_switcher"><?php /* @escapeNotVerified */ echo __('Choose Revision') ?>:</label>
<select
    id="revision_switcher"
    class="admin__control-select"
    name="revision_switcher">
    <option value=""><?php /* @escapeNotVerified */ echo __('Current Modification') ?></option>
    <?php foreach ($data as $row): ?>
        <optgroup label="<?php /* @escapeNotVerified */ echo $row['label'] ?>">
        <?php foreach ($row['revisions'] as $item): ?>
            <option value="<?php /* @escapeNotVerified */ echo $item->getId() ?>"<?php if ($block->getRevisionId() == $item->getId()): ?> selected="selected"<?php endif; ?>>&nbsp;&nbsp;&nbsp;&nbsp;<?php /* @escapeNotVerified */ echo __('Revision #%1', $item->getRevisionNumber()) ?></option>
        <?php endforeach; ?>
    <?php endforeach; ?>
</select>
<script>
require(['prototype'], function(){

//<![CDATA[
Event.observe($('revision_switcher'), 'change', function(event) {
    var element = Event.element(event);
    $('preview_selected_revision').value = element.value;
    togglePublish(element.value);
});
//]]>

});
</script>
<?php endif; ?>
