<?php
/**
 * Copyright © 2016 Magento. All rights reserved.
 * See COPYING.txt for license details.
 */
?>

<script>
require(['prototype'], function(){

//<![CDATA[
OptionTemplateCountry =
    '<div class="field field-region">'+
        '<label class="label" for=""><span><?php /* @escapeNotVerified */ echo __('Show Region') ?></label>'+
        '<div class="control"><?php echo $block->getRegionShowSelectHtml() ?></div>'+
    '</div>';

selectCountryType = {
    add : function(data) {
        if (data.show_region) {
            $A($(data.prefix+'_attribute_'+data.id+'_show_region').options).each(function(option){
                if (option.value==data.show_region) option.selected = true;
           });
        }
    }
}
//]]>

});
</script>
